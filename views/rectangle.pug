extends layout

block content
  h1= title

  -
  - var results = [{width: 15, height: 8}, {width: 100, height: 200}, {width: 4, height: 2}]

  div(class="container-fluid")
    div(class="row")
    each rectangle in results 
      div(class="col-sm-2") #{rectangle.width} #{rectangle.height}
